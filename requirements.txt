# requirements.txt — pointer for pip-only users; pyproject.toml is the source of truth.
#
#   pip install -r requirements.txt        # core + Claude Code (Agent-SDK) backend
#
# Prefer the project extras directly:
#   uv pip install -e '.[dev]'             # core + test/lint tooling (offline floor)
#   uv pip install -e '.[llm]'             # + claude-agent-sdk (claude_code backend)
#   uv pip install -e '.[sources]'         # + requests (keyed grounding sources)
#   uv pip install -e '.[all]'             # everything
#
# The core (schema + render + eval + Crossref/arXiv grounding) runs with NO extras.
-e .[llm]
